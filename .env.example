# NEXEN Environment Variables
# ============================
# 只需要三个提供商的 API 密钥即可运行

# =============================================================================
# 必需的 API 密钥 (至少配置一个)
# =============================================================================

# OpenAI API Key (用于 GPT-4o, o3)
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-key

# Anthropic API Key (用于 Claude)
# 获取地址: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Google API Key (用于 Gemini)
# 获取地址: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-google-key

# =============================================================================
# 可选配置
# =============================================================================

# 向量数据库 (可选，用于语义搜索)
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=your-qdrant-key

# 工作空间目录 (默认: ./research_workspace)
# NEXEN_WORKSPACE_DIR=/path/to/workspace

# 日志级别 (默认: INFO)
# LOG_LEVEL=DEBUG

# =============================================================================
# Web 应用配置 (可选)
# =============================================================================

# 数据库 (Web 应用使用)
# DATABASE_URL=postgresql+asyncpg://nexen:nexen@localhost:5432/nexen

# Redis (Web 应用使用)
# REDIS_URL=redis://localhost:6379/0

# 安全密钥 (生产环境必须修改)
# SECRET_KEY=your-secret-key-change-in-production
